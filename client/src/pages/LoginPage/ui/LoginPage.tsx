import { LoginCard } from '@/comp/LoginCard';

export const LoginPage = () => {
	return (
		<section className="flex h-screen w-full flex-col items-center justify-center bg-gray-200">
			<LoginCard />
		</section>
	);
};
